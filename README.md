The rules for Beyond Reach are stored in the RULES.md file.

I write the rules in a Vimwiki-flavored Markdown document, so there's a python script (`syntax-conversion.py`) to conver it to Github-Flavored Markdown.
